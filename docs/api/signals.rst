
Signals
=======

Reactive values that widgets can bind to for automatic two-way synchronization.
Pass a ``Signal`` to ``textsignal=`` (text-bearing widgets) or ``signal=``
(boolean and numeric widgets) to keep the UI in sync with application state.

.. autoclass:: bootstack.signals.signal.Signal
   :members:
   :undoc-members: