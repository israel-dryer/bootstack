# After

This capability documents one focused aspect of the **widget interface** (Tk/Tcl-style behavior + bootstack
extensions).

> **Note**: You typically won’t use `bootstack._core.capabilities.after` directly. This page describes the behavior
> that widgets expose.

::: bootstack._core.capabilities.after.AfterMixin
    options:
      filters:
        - "!^_"
