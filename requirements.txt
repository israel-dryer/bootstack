# Runtime dependencies — mirror [project.dependencies] in pyproject.toml.
# Prefer `pip install bootstack` (or `pip install -e .`); this file is for
# environments that pin from a requirements file.

pillow>=10,<=12
typing-extensions>=4.10

# Localization / formatting
Babel>=2.12
python-dateutil>=2.9
dateparser>=1.1

# Multi-monitor window placement
screeninfo>=0.8

# Windows-only window effects (mica, acrylic, …)
pywinstyles>=1.8; sys_platform == "win32"
